-- PostgreSQL: SEED — insert sample rows into walkthrough_tasks.

INSERT INTO walkthrough_tasks (title, is_done) VALUES
    ('Write project README', TRUE),
    ('Set up Docker sandbox', TRUE),
    ('Review ER diagram output', FALSE),
    ('Export schema to Excel', FALSE),
    ('Publish to GitHub', FALSE);
