<Window x:Class="NeoDbStudio.Client.Views.BlobViewerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True"
        Title="BLOB / CLOB Viewer"
        Height="560" Width="760"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        Background="#1E1E1E">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TxtHeader" Grid.Row="0" Text="🧬 BLOB / CLOB Viewer — 0 bytes" FontSize="16" FontWeight="Bold" Foreground="#1E90FF" Margin="0,0,0,10"/>

        <TabControl Grid.Row="1">
            <TabItem Header="📄 Text">
                <TextBox x:Name="TxtTextView" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                         FontFamily="Consolas" FontSize="13" Background="#252526" Foreground="#F0F0F0" BorderThickness="0"/>
            </TabItem>
            <TabItem Header="🔢 Hex">
                <TextBox x:Name="TxtHexView" IsReadOnly="True" TextWrapping="NoWrap" AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                         FontFamily="Consolas" FontSize="12" Background="#252526" Foreground="#F0F0F0" BorderThickness="0"/>
            </TabItem>
            <TabItem x:Name="TabImagePreview" Header="🖼 Image" Visibility="Collapsed">
                <Border Background="#252526">
                    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                        <Image x:Name="ImgPreview" Stretch="None" Margin="10"/>
                    </ScrollViewer>
                </Border>
            </TabItem>
        </TabControl>

        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="値はコピーのみ可能です（このビューアからの編集は反映されません）。" Foreground="#AAAAAA" FontSize="11" VerticalAlignment="Center"/>
            <Button x:Name="BtnSaveAsFile" Grid.Column="1" Content="💾 Save As..." Click="BtnSaveAsFile_Click" Height="32" Padding="12,0" Margin="0,0,8,0"/>
            <Button x:Name="BtnClose" Grid.Column="2" Content="Close" Click="BtnClose_Click" Height="32" Padding="18,0"/>
        </Grid>
    </Grid>
</Window>
